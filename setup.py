from setuptools import setup, find_packages

setup(
    name="data-pipeline",
    version="1.0.0",
    packages=find_packages(),
    author="Developer",
    description="Python data pipeline framework",
    python_requires=">=3.8",
)
